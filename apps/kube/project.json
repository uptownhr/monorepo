{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/kube/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/kube",
        "main": "apps/kube/src/main.ts",
        "tsConfig": "apps/kube/tsconfig.app.json",
        "assets": ["apps/kube/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/kube/src/environments/environment.ts",
              "with": "apps/kube/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "kube:build"
      },
      "configurations": {
        "production": {
          "buildTarget": "kube:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/kube/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/kube"],
      "options": {
        "jestConfig": "apps/kube/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
